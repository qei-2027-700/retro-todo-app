# ============================================
# 開発環境用 Dockerfile（Air使用）
# ============================================
FROM golang:1.24-alpine

WORKDIR /app

# 開発ツールのインストール
# Go 1.24対応バージョンを指定
RUN go install github.com/air-verse/air@v1.61.7

# 依存関係のダウンロード
COPY go.mod go.sum ./
RUN go mod download

# ソースコードをマウントするため、ここではコピーしない
# docker-compose.ymlでvolumesマウントを使用

# Airで起動
CMD ["air", "-c", ".air.toml"]
